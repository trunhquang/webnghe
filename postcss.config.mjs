/// The "Spelling Police". It underlines your code in red if you write bad or non-standard code, helping keep the codebase clean.

const config = {
  // Specify the PostCSS plugins you want to use
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
